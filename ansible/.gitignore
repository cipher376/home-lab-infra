# Ignore the vault password file
.vault_pass
.vault_pass*
.vault_key
setup_terraform_env.sh
# Ignore temporary retry files
*.retry

# Ignore terraform state (we will move this to GitLab later)
.terraform/
terraform.tfstate*
vars/*
hc-vault_keys.json
.venv

# =============================================================================
# Ansible venv
# =============================================================================
.venv/
*.venv/
 
# =============================================================================
# Secrets — never commit these
# =============================================================================
hc-vault_keys.json
vars/secrets.yaml
.vault_key
*.vault_key
 
# =============================================================================
# Ansible artifacts
# =============================================================================
*.retry
*.log
 